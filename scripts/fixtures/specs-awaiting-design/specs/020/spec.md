# Spec 020 — synthetic (design merged, negative control)

Fixture spec that has a merged design-a.md and a STATUS row at `design
approved`. The gauge must NOT count it. Its presence proves the watcher excludes
specs whose design has landed, rather than counting every spec.md.
