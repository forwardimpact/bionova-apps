# Spec 030 — synthetic (past design, negative control)

Fixture spec with no design-a.md but a STATUS row at `plan draft`. It has moved
beyond the design phase, so the gauge must NOT count it. This proves the watcher
reads the ledger phase, not just the absence of a design file.
