# Spec 010 — synthetic (awaiting design)

Fixture spec for the spec→design watcher. Merged with no design-a.md and a
STATUS row at `design draft`, so it is the one spec the gauge must count. Its
`merged_at` sits before the two-day clock, so it also lands in `flagged`.

This is fixture content only. It is not a real Polaris feature.
